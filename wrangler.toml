name = "cloudflare-proxy"
main = "src/index.ts"
compatibility_date = "2025-06-24"

# Build CI : configurer dans Workers Builds → "Build command" = npm run build
# (pas de [build] ici : wrangler deploy le relancerait et doublerait l'install)

# wrangler secret put API_KEY